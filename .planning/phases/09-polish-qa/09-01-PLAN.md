---
phase: 09-polish-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart
  - mobile/test/widget/notifications_screen_test.dart
  - mobile/test/unit/core/sync/sync_service_stale_video_test.dart
  - mobile/test/unit/features/coach_chat/fcm_service_test.dart
  - mobile/test/unit/features/metrics/metric_delta_test.dart
  - mobile/test/unit/features/metrics/offline_metric_sync_test.dart
  - mobile/test/unit/features/session/offline_sync_integration_test.dart
  - mobile/test/widget/metric_log_bottom_sheet_test.dart
  - mobile/test/widget/progress_screen_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "flutter analyze --fatal-infos exits 0 with zero issues"
    - "flutter test passes with no regressions"
  artifacts:
    - path: "mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart"
      provides: "Deprecated value → initialValue migration"
      contains: "initialValue:"
    - path: "mobile/test/widget/notifications_screen_test.dart"
      provides: "Removed unused_local_variable warning"
  key_links:
    - from: "mobile/test/unit/core/sync/sync_service_stale_video_test.dart"
      to: "package:supabase_flutter"
      via: "Transitive import only (no direct postgrest import)"
      pattern: "no postgrest import"
---

<objective>
Fix all 15 static analysis issues (14 infos + 1 warning) so `flutter analyze --fatal-infos` exits 0.

Purpose: SC-P9-1 requires zero errors and zero warnings from the analyzer. This is the gating prerequisite for all subsequent plans.
Output: Clean static analysis pass with all 15 issues resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-qa/09-RESEARCH.md
@mobile/analysis_options.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deprecated DropdownButtonFormField.value and remove unused variable</name>
  <files>
    mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart
    mobile/test/widget/notifications_screen_test.dart
  </files>
  <read_first>
    mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart
    mobile/test/widget/notifications_screen_test.dart
  </read_first>
  <action>
1. In `metric_log_bottom_sheet.dart`:
   - Line 184: change `value: _selectedType` to `initialValue: _selectedType`
   - Line 213: change `value: _selectedSubtype` to `initialValue: _selectedSubtype`
   (Both are DropdownButtonFormField widgets where `value` is deprecated after Flutter 3.33.0-1.0.pre; the replacement is `initialValue`)

2. In `test/widget/notifications_screen_test.dart`:
   - Line 41: remove the `final recorded = <String>[];` declaration entirely (unused_local_variable warning). If it is referenced elsewhere in the file, remove only if truly unused; if used later, investigate and keep.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze --fatal-infos 2>&1 | grep -c "issue\|warning\|error" || true</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "initialValue: _selectedType" mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart
    - grep -q "initialValue: _selectedSubtype" mobile/lib/features/metrics/presentation/metric_log_bottom_sheet.dart
    - ! grep -q "final recorded" mobile/test/widget/notifications_screen_test.dart
  </acceptance_criteria>
  <done>Both deprecated_member_use infos eliminated; unused_local_variable warning eliminated. 3 issues fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove redundant postgrest imports and fix leading underscore identifiers</name>
  <files>
    mobile/test/unit/core/sync/sync_service_stale_video_test.dart
    mobile/test/unit/features/coach_chat/fcm_service_test.dart
    mobile/test/unit/features/metrics/metric_delta_test.dart
    mobile/test/unit/features/metrics/offline_metric_sync_test.dart
    mobile/test/unit/features/session/offline_sync_integration_test.dart
    mobile/test/widget/metric_log_bottom_sheet_test.dart
    mobile/test/widget/progress_screen_test.dart
  </files>
  <read_first>
    mobile/test/unit/core/sync/sync_service_stale_video_test.dart
    mobile/test/unit/features/coach_chat/fcm_service_test.dart
    mobile/test/unit/features/metrics/metric_delta_test.dart
    mobile/test/unit/features/metrics/offline_metric_sync_test.dart
    mobile/test/unit/features/session/offline_sync_integration_test.dart
    mobile/test/widget/metric_log_bottom_sheet_test.dart
    mobile/test/widget/progress_screen_test.dart
  </read_first>
  <action>
1. Remove direct `package:postgrest/postgrest.dart` imports from these test files (they get postgrest transitively via supabase_flutter):
   - `test/unit/core/sync/sync_service_stale_video_test.dart` — remove line 5 (import of postgrest) and line 7 (import of supabase if it is also a direct non-flutter import)
   - `test/unit/features/coach_chat/fcm_service_test.dart` — remove line 8 (postgrest import)
   - `test/unit/features/metrics/offline_metric_sync_test.dart` — remove line 7 (postgrest import)
   - `test/unit/features/session/offline_sync_integration_test.dart` — remove line 7 (postgrest import)

   After removing these imports, check if any types from postgrest are used (e.g., PostgrestFilterBuilder). If so, keep only the necessary import or get it through supabase_flutter re-export. The research indicates these are unnecessary imports, so removing should be safe.

2. Rename leading-underscore local identifiers to remove the underscore:
   - `test/unit/features/metrics/metric_delta_test.dart` line 7: rename `_makeLog` to `makeLog` and update all call sites in that file (lines 25, 31, 32, 39, 40, 47, 48)
   - `test/widget/metric_log_bottom_sheet_test.dart` line 45: rename `_buildSubject` to `buildSubject` and update all call sites (lines 59, 66, 72, 78)
   - `test/widget/progress_screen_test.dart` line 21: rename `_buildSubject` to `buildSubject` and update all call sites (line 57 and any others)

3. Verify: `cd mobile && flutter analyze --fatal-infos` must exit 0.
4. Verify: `cd mobile && flutter test test/` must still pass (no regressions from import removal or renames).
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze --fatal-infos && flutter test test/ --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -q "package:postgrest" mobile/test/unit/core/sync/sync_service_stale_video_test.dart
    - ! grep -q "package:postgrest" mobile/test/unit/features/coach_chat/fcm_service_test.dart
    - ! grep -q "package:postgrest" mobile/test/unit/features/metrics/offline_metric_sync_test.dart
    - ! grep -q "package:postgrest" mobile/test/unit/features/session/offline_sync_integration_test.dart
    - grep -q "makeLog" mobile/test/unit/features/metrics/metric_delta_test.dart
    - ! grep -q "_makeLog" mobile/test/unit/features/metrics/metric_delta_test.dart
    - grep -q "buildSubject" mobile/test/widget/metric_log_bottom_sheet_test.dart
    - ! grep -q "_buildSubject" mobile/test/widget/metric_log_bottom_sheet_test.dart
    - grep -q "buildSubject" mobile/test/widget/progress_screen_test.dart
    - ! grep -q "_buildSubject" mobile/test/widget/progress_screen_test.dart
    - cd mobile && flutter analyze --fatal-infos exits 0
    - cd mobile && flutter test test/ exits 0
  </acceptance_criteria>
  <done>All 12 remaining analyzer issues fixed (8 redundant imports removed, 3 identifiers renamed). flutter analyze --fatal-infos exits 0. flutter test passes with zero regressions.</done>
</task>

</tasks>

<verification>
cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze --fatal-infos
cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test test/ --reporter compact
Both must exit 0.
</verification>

<success_criteria>
- `flutter analyze --fatal-infos` exits 0 (zero issues at any severity)
- `flutter test test/` exits 0 (all existing 111+ tests pass)
- No new files created; only existing files modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-qa/09-01-SUMMARY.md`
</output>
