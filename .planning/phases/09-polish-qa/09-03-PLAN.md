---
phase: 09-polish-qa
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - mobile/integration_test/sc001_onboarding_time_test.dart
  - mobile/integration_test/sc002_video_playback_time_test.dart
  - mobile/integration_test/sc003_model_load_time_test.dart
  - mobile/integration_test/sc004_offline_sync_time_test.dart
  - mobile/integration_test/sc005_admin_publish_manual.dart
  - mobile/integration_test/sc006_push_notification_time_test.dart
  - mobile/integration_test/sc007_app_rating_kpi.dart
  - mobile/integration_test/sc008_retention_kpi.dart
  - mobile/lib/core/analytics/analytics_service.dart
  - mobile/lib/core/analytics/analytics_provider.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "SC-001..SC-008 integration test stubs exist and pass (via skip or assertion)"
    - "AnalyticsService abstraction exists with NoOp implementation"
    - "analyticsServiceProvider is registered and returns NoOpAnalyticsService"
  artifacts:
    - path: "mobile/integration_test/sc001_onboarding_time_test.dart"
      provides: "SC-001 benchmark test stub"
      contains: "IntegrationTestWidgetsFlutterBinding"
    - path: "mobile/integration_test/sc008_retention_kpi.dart"
      provides: "SC-008 KPI stub"
      contains: "skip:"
    - path: "mobile/lib/core/analytics/analytics_service.dart"
      provides: "Abstract AnalyticsService + NoOpAnalyticsService"
      contains: "NoOpAnalyticsService"
    - path: "mobile/lib/core/analytics/analytics_provider.dart"
      provides: "Riverpod provider for analytics service"
      contains: "analyticsServiceProvider"
  key_links:
    - from: "mobile/lib/core/analytics/analytics_provider.dart"
      to: "mobile/lib/core/analytics/analytics_service.dart"
      via: "import and instantiation"
      pattern: "NoOpAnalyticsService"
---

<objective>
Create SC-001..SC-008 integration test stubs and an analytics service scaffold (NoOp implementation).

Purpose: SC-P9-2 requires benchmark integration tests to exist. The analytics scaffold prepares for future event tracking without adding external dependencies.
Output: 8 integration test files + analytics abstraction layer with Riverpod provider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-qa/09-RESEARCH.md
@mobile/integration_test/auth_login_test.dart
</context>

<interfaces>
<!-- Integration test pattern from existing auth_login_test.dart: -->
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();
  testWidgets('description', (tester) async {
    // ...
  }, skip: 'Requires device + credentials');
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SC-001..SC-008 integration test stubs</name>
  <files>
    mobile/integration_test/sc001_onboarding_time_test.dart
    mobile/integration_test/sc002_video_playback_time_test.dart
    mobile/integration_test/sc003_model_load_time_test.dart
    mobile/integration_test/sc004_offline_sync_time_test.dart
    mobile/integration_test/sc005_admin_publish_manual.dart
    mobile/integration_test/sc006_push_notification_time_test.dart
    mobile/integration_test/sc007_app_rating_kpi.dart
    mobile/integration_test/sc008_retention_kpi.dart
  </files>
  <read_first>
    mobile/integration_test/auth_login_test.dart
  </read_first>
  <action>
Create 8 integration test files following the pattern from `auth_login_test.dart`. Each file:
- Imports `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`
- Calls `IntegrationTestWidgetsFlutterBinding.ensureInitialized()` at the top of main()
- Contains one `testWidgets` with descriptive name and a `skip:` string

Specific file contents:

**sc001_onboarding_time_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  testWidgets('SC-001: signup + subscribe + first session start < 3 minutes', (tester) async {
    // Arrange: cold launch
    // Act: complete signup, subscribe, navigate to first session
    // Assert: total elapsed < 180 seconds
    final stopwatch = Stopwatch()..start();
    // TODO: app.main(), complete signup flow, subscribe, open session
    stopwatch.stop();
    expect(stopwatch.elapsedMilliseconds, lessThan(180000));
  }, skip: 'Requires device + Supabase credentials + RevenueCat sandbox — run via ./local-dev/test-sc001.sh');
}
```

**sc002_video_playback_time_test.dart:**
- Test name: 'SC-002: video playback starts within 2s (pre-downloaded)'
- Stopwatch assertion: `expect(stopwatch.elapsedMilliseconds, lessThan(2000))`
- Skip message: 'Requires device + pre-downloaded video content'

**sc003_model_load_time_test.dart:**
- Test name: 'SC-003: 3D animation loads within 1s of session screen open'
- Stopwatch assertion: `expect(stopwatch.elapsedMilliseconds, lessThan(1000))`
- Skip message: 'Requires device + downloaded GLB model'

**sc004_offline_sync_time_test.dart:**
- Test name: 'SC-004: offline completion syncs within 10s of reconnection'
- Stopwatch assertion: `expect(stopwatch.elapsedMilliseconds, lessThan(10000))`
- Skip message: 'Requires device + network toggle capability'

**sc005_admin_publish_manual.dart:**
- Test name: 'SC-005: coach creates + publishes program in under 15 minutes'
- No Stopwatch needed — this is a manual verification stub
- Skip message: 'Manual verification — admin panel E2E not automatable in Flutter integration tests'
- Include a comment documenting the manual test steps: create program, add session, upload video, publish, verify in student app

**sc006_push_notification_time_test.dart:**
- Test name: 'SC-006: push notification delivered within 60s of coach reply'
- Stopwatch assertion: `expect(stopwatch.elapsedMilliseconds, lessThan(60000))`
- Skip message: 'Requires device + Firebase Cloud Messaging credentials + admin panel'

**sc007_app_rating_kpi.dart:**
- Test name: 'SC-007: App Store rating target >= 4.5 stars'
- No assertion — KPI tracked post-launch
- Skip message: 'KPI metric — tracked via App Store Connect / Play Console post-launch'

**sc008_retention_kpi.dart:**
- Test name: 'SC-008: 30-day retention rate target >= 60%'
- No assertion — KPI tracked post-launch
- Skip message: 'KPI metric — tracked via analytics dashboard post-launch'

Each file should have a brief header comment documenting what SC criterion it covers and what success looks like.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test integration_test/ --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - test -f mobile/integration_test/sc001_onboarding_time_test.dart
    - test -f mobile/integration_test/sc002_video_playback_time_test.dart
    - test -f mobile/integration_test/sc003_model_load_time_test.dart
    - test -f mobile/integration_test/sc004_offline_sync_time_test.dart
    - test -f mobile/integration_test/sc005_admin_publish_manual.dart
    - test -f mobile/integration_test/sc006_push_notification_time_test.dart
    - test -f mobile/integration_test/sc007_app_rating_kpi.dart
    - test -f mobile/integration_test/sc008_retention_kpi.dart
    - grep -q "IntegrationTestWidgetsFlutterBinding" mobile/integration_test/sc001_onboarding_time_test.dart
    - grep -q "skip:" mobile/integration_test/sc001_onboarding_time_test.dart
    - grep -q "lessThan(180000)" mobile/integration_test/sc001_onboarding_time_test.dart
    - grep -q "lessThan(2000)" mobile/integration_test/sc002_video_playback_time_test.dart
    - grep -q "lessThan(1000)" mobile/integration_test/sc003_model_load_time_test.dart
    - grep -q "lessThan(10000)" mobile/integration_test/sc004_offline_sync_time_test.dart
    - grep -q "skip:" mobile/integration_test/sc005_admin_publish_manual.dart
    - grep -q "skip:" mobile/integration_test/sc006_push_notification_time_test.dart
    - grep -q "skip:" mobile/integration_test/sc007_app_rating_kpi.dart
    - cd mobile && flutter test integration_test/ exits 0
  </acceptance_criteria>
  <done>All 8 SC-001..SC-008 integration test stubs exist in integration_test/ directory. Tests pass (skipped) when run with flutter test integration_test/.</done>
</task>

<task type="auto">
  <name>Task 2: Create AnalyticsService abstraction with NoOp implementation and Riverpod provider</name>
  <files>
    mobile/lib/core/analytics/analytics_service.dart
    mobile/lib/core/analytics/analytics_provider.dart
  </files>
  <read_first>
    mobile/lib/core/database/providers.dart
  </read_first>
  <action>
1. Create `mobile/lib/core/analytics/analytics_service.dart`:
```dart
/// Analytics service abstraction for event tracking.
///
/// Phase 9 scaffold — uses NoOpAnalyticsService.
/// Replace with a real implementation (Firebase Analytics, Mixpanel, etc.)
/// when analytics provider is selected.
abstract class AnalyticsService {
  /// Log a named event with optional parameters.
  void logEvent(String name, {Map<String, Object>? parameters});

  /// Set the current user ID for analytics attribution.
  void setUserId(String? userId);
}

/// No-op implementation that discards all events.
/// Used as scaffold until a real analytics provider is integrated.
class NoOpAnalyticsService implements AnalyticsService {
  const NoOpAnalyticsService();

  @override
  void logEvent(String name, {Map<String, Object>? parameters}) {}

  @override
  void setUserId(String? userId) {}
}
```

2. Create `mobile/lib/core/analytics/analytics_provider.dart`:
```dart
import 'package:riverpod_annotation/riverpod_annotation.dart';
import 'analytics_service.dart';

part 'analytics_provider.g.dart';

/// Provides the global [AnalyticsService] instance.
///
/// Currently returns [NoOpAnalyticsService]. Swap implementation
/// when a real analytics provider (Firebase Analytics, Mixpanel, etc.)
/// is integrated.
@Riverpod(keepAlive: true)
AnalyticsService analyticsService(AnalyticsServiceRef ref) {
  return const NoOpAnalyticsService();
}
```

3. Run `dart run build_runner build --delete-conflicting-outputs` from the mobile directory to generate the `.g.dart` file for the Riverpod provider.

4. Run `flutter analyze --fatal-infos` to verify clean compilation.

NOTE: Do NOT add analytics call sites to existing screens in this plan. The scaffold just proves the abstraction compiles and the provider is accessible. Call sites can be added in a future phase.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && flutter analyze --fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - test -f mobile/lib/core/analytics/analytics_service.dart
    - test -f mobile/lib/core/analytics/analytics_provider.dart
    - test -f mobile/lib/core/analytics/analytics_provider.g.dart
    - grep -q "NoOpAnalyticsService" mobile/lib/core/analytics/analytics_service.dart
    - grep -q "analyticsService" mobile/lib/core/analytics/analytics_provider.dart
    - grep -q "@Riverpod(keepAlive: true)" mobile/lib/core/analytics/analytics_provider.dart
    - cd mobile && flutter analyze --fatal-infos exits 0
  </acceptance_criteria>
  <done>AnalyticsService abstraction exists with NoOpAnalyticsService and a keepAlive Riverpod provider. Code compiles cleanly. No external dependency added.</done>
</task>

</tasks>

<verification>
cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze --fatal-infos && flutter test test/ --reporter compact && flutter test integration_test/ --reporter compact
All three commands must exit 0.
</verification>

<success_criteria>
- All 8 integration test files exist in `integration_test/` and pass (all skipped)
- `AnalyticsService` + `NoOpAnalyticsService` exist with generated provider
- `flutter analyze --fatal-infos` exits 0
- `flutter test` exits 0 (unit/widget + integration)
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-qa/09-03-SUMMARY.md`
</output>
