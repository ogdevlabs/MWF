---
phase: 08-us6-admin-panel
plan: 05
type: execute
wave: 4
depends_on: [08-02, 08-03, 08-04]
files_modified:
  - admin/app/(admin)/programs/[id]/page.tsx
  - admin/app/(admin)/page.tsx
  - admin/app/page.tsx
autonomous: true
requirements: [FR-017, FR-019]

must_haves:
  truths:
    - "Coach can toggle publish/unpublish on program detail page"
    - "Published program is immediately visible in student_app (regular views, not materialized)"
    - "Admin root (/) redirects to /programs"
    - "Dashboard shows summary of pending feedback count and total programs"
    - "Video version increment from Plan 04 ensures student cache invalidation (FR-019 verification)"
  artifacts:
    - path: "admin/app/(admin)/programs/[id]/page.tsx"
      provides: "Program detail with publish toggle integrated with sessions list"
      contains: "publishProgram"
    - path: "admin/app/(admin)/page.tsx"
      provides: "Admin dashboard with quick stats"
      contains: "programs"
    - path: "admin/app/page.tsx"
      provides: "Root redirect to /programs"
      contains: "redirect"
  key_links:
    - from: "admin/app/(admin)/programs/[id]/page.tsx"
      to: "admin/app/actions/programs.ts"
      via: "publishProgram/unpublishProgram server actions"
      pattern: "publishProgram\\|unpublishProgram"
    - from: "programs.published = true"
      to: "student app visibility"
      via: "CQRS regular views (security_invoker=true) reflect changes instantly"
      pattern: "published"
---

<objective>
Wire up the final integration points: publish/unpublish toggle on the program detail page, admin dashboard with quick stats, and root redirect. Verify FR-017 (publish = instant visibility) and FR-019 (video version = cache invalidation) are complete end-to-end.

Purpose: Final integration ensuring the admin panel is complete and all 5 requirements (FR-015 through FR-019) are satisfied.
Output: Complete admin panel ready for UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-us6-admin-panel/08-RESEARCH.md
@.planning/phases/08-us6-admin-panel/08-01-SUMMARY.md
@.planning/phases/08-us6-admin-panel/08-02-SUMMARY.md
@.planning/phases/08-us6-admin-panel/08-03-SUMMARY.md
@.planning/phases/08-us6-admin-panel/08-04-SUMMARY.md
@admin/app/actions/programs.ts
@admin/app/(admin)/programs/[id]/page.tsx
</context>

<interfaces>
<!-- From admin/app/actions/programs.ts (Plan 02) -->
```typescript
export async function publishProgram(programId: string): Promise<void>
export async function unpublishProgram(programId: string): Promise<void>
```

<!-- From admin/app/actions/exercises.ts (Plan 04) -->
```typescript
export async function updateExerciseVideo(exerciseId: string, muxAssetId: string, programId: string, sessionId: string): Promise<void>
// Increments video_version — FR-019
```

<!-- From admin/lib/supabase/service.ts (Plan 01) -->
```typescript
export function createServiceClient()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page + root redirect + publish/unpublish integration verification</name>
  <files>
    admin/app/(admin)/page.tsx,
    admin/app/page.tsx,
    admin/app/(admin)/programs/[id]/page.tsx
  </files>
  <read_first>
    admin/app/(admin)/programs/[id]/page.tsx,
    admin/app/actions/programs.ts,
    admin/app/(admin)/layout.tsx,
    admin/lib/supabase/service.ts
  </read_first>
  <action>
1. Update `admin/app/page.tsx` to redirect to /programs:
   - Replace the entire placeholder content with a simple redirect:
   ```typescript
   import { redirect } from 'next/navigation'
   export default function Home() {
     redirect('/programs')
   }
   ```

2. Create `admin/app/(admin)/page.tsx` (RSC — admin dashboard):
   - Fetch quick stats using service client:
     - Total programs count: `supabase.from('programs').select('id', { count: 'exact', head: true })`
     - Published programs count: same with `.eq('published', true)`
     - Pending feedback count: `supabase.from('feedback_threads').select('id', { count: 'exact', head: true }).is('coach_reply', null)`
   - Render dashboard cards (shadcn Card):
     - "Total Programs" card with count
     - "Published" card with count
     - "Pending Feedback" card with count and link to /feedback
   - Quick action links: "Create Program" -> /programs/new, "View Feedback" -> /feedback
   - NOTE: since the admin layout has /programs and /feedback in sidebar, the dashboard at / within (admin) group serves as a landing page. The root `app/page.tsx` (outside route groups) redirects there.

3. Update `admin/app/(admin)/programs/[id]/page.tsx`:
   The publish toggle is already present on the program detail page from Plan 02. This task:
   (1) Adds an "Add Session" link on the program detail page pointing to `/programs/[id]/sessions/new` — render as a shadcn Button with `<Link href={`/programs/${program.id}/sessions/new`}>Add Session</Link>`
   (2) Adds `published_at` timestamp display next to the publish toggle — if `program.published_at`, render a formatted date string: `Published on {new Date(program.published_at).toLocaleDateString()}`
   (3) Verifies the dashboard stats card shows correct counts from the DB — ensure the service client query uses `{ count: 'exact', head: true }` pattern for efficient counting

   FR-017 verification note: Since CQRS views are regular views (not materialized), changing `published = true` in the programs table is instantly reflected in `program_catalog_view`. The student app's next fetch will see the program. The 60-second SLA is easily met because there is no materialization lag.

   FR-019 verification note: `updateExerciseVideo` in exercises.ts (Plan 04) increments `video_version`. The mobile app's `download_manifest` table stores the version — when versions differ on next sync, the app queues a re-download. This is wired end-to-end.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/admin && npm run build && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `admin/app/page.tsx` contains `redirect('/programs')` (no placeholder content)
    - `admin/app/(admin)/page.tsx` exists with dashboard showing program count, published count, pending feedback count
    - Dashboard uses shadcn Card components
    - `admin/app/(admin)/programs/[id]/page.tsx` has publish/unpublish form action
    - Program detail shows "Published"/"Draft" Badge
    - Program detail shows `published_at` timestamp when published
    - Program detail shows "Add Session" link pointing to `/programs/[id]/sessions/new`
    - `npm run build` passes with zero errors
  </acceptance_criteria>
  <done>Admin panel fully integrated: root redirects to /programs, dashboard shows stats, program publish toggle works with timestamp display, Add Session link present, all FR-015 through FR-019 pathways are complete.</done>
</task>

</tasks>

<verification>
- `cd admin && npm run build` passes with zero errors
- Root / redirects to /programs (no placeholder page)
- Dashboard shows quick stats (program count, feedback count)
- Publish/unpublish toggle present on program detail with published_at display
- "Add Session" link on program detail page
- All routes build and resolve correctly
- No `middleware.ts` exists anywhere (only `proxy.ts`)
</verification>

<success_criteria>
- Build passes: `cd admin && npm run build` exits 0
- Admin panel is complete end-to-end for all 5 requirements
- FR-015: CRUD programs (Plan 02)
- FR-016: Sessions + exercises + video + GLB (Plan 04)
- FR-017: Publish/unpublish with instant visibility (this plan)
- FR-018: Feedback reply with FCM push (Plan 03)
- FR-019: video_version increment invalidates student caches (Plan 04 + this plan confirms wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/08-us6-admin-panel/08-05-SUMMARY.md`
</output>
