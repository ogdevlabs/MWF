---
plan: 01-01
phase: 1
wave: 1
title: "Project Initializations (Flutter, Next.js, Supabase)"
depends_on: []
files_modified:
  - mobile/pubspec.yaml
  - mobile/lib/main.dart
  - mobile/android/app/build.gradle
  - mobile/ios/Runner/Info.plist
  - admin/package.json
  - admin/app/layout.tsx
  - admin/app/page.tsx
  - admin/next.config.ts
  - supabase/config.toml
autonomous: true
requirements_addressed: []
---

<objective>
Initialize the three independent project trees (Flutter mobile app, Next.js admin panel, Supabase backend) from scratch using CLI tooling.

Purpose: Establish the directory structure and baseline configuration that all subsequent plans depend on. These three projects have zero shared state and can be created fully in parallel.

Output: `mobile/`, `admin/`, and `supabase/` directories exist with valid project skeletons that pass their respective smoke checks (`flutter analyze`, `npm run build`, file existence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-scaffold/01-RESEARCH.md
@specs/001-mat-pilates-coach/data-model.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create feature branch for Phase 1</name>
  <files></files>
  <action>
Create the feature branch for all Phase 1 work:
```bash
git checkout -b phase/01-setup-scaffold
```
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "phase/01-setup-scaffold" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` returns `phase/01-setup-scaffold`
  </acceptance_criteria>
  <done>Feature branch `phase/01-setup-scaffold` is created and checked out.</done>
</task>

<task type="auto">
  <name>Task 1: Initialize Flutter project at mobile/</name>
  <files>
    mobile/pubspec.yaml
    mobile/lib/main.dart
    mobile/android/app/build.gradle
    mobile/android/settings.gradle
    mobile/ios/Runner/Info.plist
    mobile/ios/Podfile
    mobile/test/widget_test.dart
    mobile/analysis_options.yaml
  </files>
  <read_first>
    .planning/phases/01-setup-scaffold/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <action>
1. Accept Android licenses (required before any Android build):
   ```bash
   flutter doctor --android-licenses
   ```
   (Answer "y" to all prompts)

2. Create Flutter project from repo root:
   ```bash
   flutter create --org com.fererelabs --project-name mwf_mobile mobile
   ```

3. Verify the project was created:
   ```bash
   ls mobile/pubspec.yaml mobile/lib/main.dart
   ```

4. Run `flutter analyze` from `mobile/` to confirm zero errors on the freshly generated project:
   ```bash
   cd mobile && flutter analyze
   ```

This generates the full Flutter project skeleton including `android/`, `ios/`, `lib/`, `test/`, `pubspec.yaml`, and `analysis_options.yaml`. The default counter app in `main.dart` will be replaced in Plan 01-03.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - mobile/pubspec.yaml exists and contains "name: mwf_mobile"
    - mobile/lib/main.dart exists
    - mobile/android/app/build.gradle exists
    - mobile/ios/Runner/Info.plist exists
    - `flutter analyze` from mobile/ exits with code 0
  </acceptance_criteria>
  <done>Flutter project skeleton exists at mobile/ and passes `flutter analyze` with zero issues.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Next.js 16 admin panel at admin/</name>
  <files>
    admin/package.json
    admin/app/layout.tsx
    admin/app/page.tsx
    admin/next.config.ts
    admin/tailwind.config.ts
    admin/tsconfig.json
  </files>
  <read_first>
    .planning/phases/01-setup-scaffold/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <action>
1. Create Next.js project from repo root using create-next-app (installs Next.js 16.x, NOT 15):
   ```bash
   npx create-next-app@latest admin --typescript --tailwind --app --eslint --yes
   ```
   Flags: `--app` (App Router), `--typescript`, `--tailwind` (Tailwind CSS), `--eslint`, `--yes` (accept defaults non-interactively).

2. Verify the project was created:
   ```bash
   ls admin/package.json admin/app/layout.tsx admin/next.config.ts
   ```

3. Run build to confirm zero errors:
   ```bash
   cd admin && npm run build
   ```

IMPORTANT: This installs Next.js 16.x (not 15). Key differences from tutorials:
- `params` in page components must be `Promise<{...}>` and `await`-ed
- `cookies()` and `headers()` from `next/headers` must be `await`-ed
- `middleware.ts` is deprecated; use `proxy.ts` (but middleware still works for now)
- Default bundler is Turbopack

Node.js requirement: 20.9+ (this machine has 22.22.3 — satisfied).
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/admin && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - admin/package.json exists and contains "next" in dependencies
    - admin/app/layout.tsx exists
    - admin/next.config.ts exists
    - `npm run build` from admin/ exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project skeleton exists at admin/ with App Router, TypeScript, Tailwind, and passes `npm run build`.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Supabase project at supabase/</name>
  <files>
    supabase/config.toml
    supabase/.gitignore
  </files>
  <read_first>
    .planning/phases/01-setup-scaffold/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <action>
1. Initialize Supabase project from repo root:
   ```bash
   npx supabase init
   ```
   This creates `supabase/config.toml` and `supabase/.gitignore`.

2. Create the migrations directory (not auto-created by `supabase init`):
   ```bash
   mkdir -p supabase/migrations
   ```

3. Create the functions directory (for edge functions in Plan 01-02):
   ```bash
   mkdir -p supabase/functions
   ```

4. Verify structure:
   ```bash
   ls supabase/config.toml supabase/migrations/ supabase/functions/
   ```

NOTE: Do NOT run `supabase init` from inside `supabase/` — run from repo root. The CLI creates the `supabase/` directory itself.
  </action>
  <verify>
    <automated>test -f /Users/oglabs/source/fererelabs/mwf/supabase/config.toml && test -d /Users/oglabs/source/fererelabs/mwf/supabase/migrations && test -d /Users/oglabs/source/fererelabs/mwf/supabase/functions && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/config.toml exists
    - supabase/migrations/ directory exists
    - supabase/functions/ directory exists
  </acceptance_criteria>
  <done>Supabase project skeleton exists at supabase/ with config.toml, migrations/, and functions/ directories ready for subsequent plans.</done>
</task>

</tasks>

<verification>
All three project trees exist and pass their smoke checks:
1. `cd mobile && flutter analyze` — exits 0
2. `cd admin && npm run build` — exits 0
3. `test -f supabase/config.toml` — true
</verification>

<success_criteria>
- Feature branch `phase/01-setup-scaffold` is checked out
- Three independent project directories exist: mobile/, admin/, supabase/
- Flutter project compiles and analyzes cleanly
- Next.js project builds without errors
- Supabase config.toml exists with migrations/ and functions/ directories
- No files committed that should be gitignored (node_modules, build outputs)
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-scaffold/01-01-SUMMARY.md`
</output>

---
must_haves:
  truths:
    - "mobile/ directory contains a valid Flutter project that passes flutter analyze"
    - "admin/ directory contains a valid Next.js 16 project that passes npm run build"
    - "supabase/ directory contains config.toml and empty migrations/ and functions/ directories"
  artifacts:
    - path: "mobile/pubspec.yaml"
      provides: "Flutter project manifest"
      contains: "name: mwf_mobile"
    - path: "admin/package.json"
      provides: "Next.js project manifest"
      contains: "next"
    - path: "supabase/config.toml"
      provides: "Supabase local project configuration"
  key_links: []
verification_steps:
  - "cd mobile && flutter analyze exits 0"
  - "cd admin && npm run build exits 0"
  - "test -f supabase/config.toml && test -d supabase/migrations"
---
</output>
