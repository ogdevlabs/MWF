---
phase: 07-us5-private-feedback
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - mobile/lib/features/coach_chat/presentation/coach_tab_screen.dart
  - mobile/lib/features/coach_chat/presentation/coach_paywall_screen.dart
  - mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart
  - mobile/lib/features/coach_chat/presentation/widgets/chat_bubble.dart
  - mobile/lib/features/coach_chat/presentation/widgets/compose_bar.dart
  - mobile/lib/features/coach_chat/presentation/widgets/photo_thumbnail.dart
  - mobile/test/widget/coach_chat_screen_test.dart
autonomous: true
requirements: [FR-010, FR-010a]

must_haves:
  truths:
    - "CoachTabScreen gates on isSubscribedProvider: premium shows CoachChatScreen, basic shows CoachPaywallScreen"
    - "CoachPaywallScreen displays lock icon, heading, description, and Upgrade to Premium CTA"
    - "CoachChatScreen renders chat bubbles from feedbackThreadProvider stream"
    - "Student bubbles are right-aligned with primaryContainer background"
    - "Coach bubbles are left-aligned with surfaceContainerHighest background"
    - "Pending messages show clock icon (Icons.schedule) beside timestamp"
    - "Compose bar has TextField, camera icon button, and send button"
    - "Send button disabled when text is empty and no photo attached"
    - "Photo thumbnail renders at 160x120 with tap-to-expand via InteractiveViewer"
    - "Welcome message shown when no real messages exist"
  artifacts:
    - path: "mobile/lib/features/coach_chat/presentation/coach_tab_screen.dart"
      provides: "Premium gate routing between chat and paywall"
      contains: "isSubscribedProvider"
    - path: "mobile/lib/features/coach_chat/presentation/coach_paywall_screen.dart"
      provides: "Paywall CTA screen for non-premium users"
      contains: "Upgrade to Premium"
    - path: "mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart"
      provides: "iMessage-style chat thread with compose bar"
      contains: "ComposeBar"
      min_lines: 80
    - path: "mobile/lib/features/coach_chat/presentation/widgets/chat_bubble.dart"
      provides: "Reusable bubble widget with coach/student variants"
      contains: "isCoach"
    - path: "mobile/lib/features/coach_chat/presentation/widgets/compose_bar.dart"
      provides: "TextField + camera + send button bar"
      contains: "Message coach"
  key_links:
    - from: "mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart"
      to: "mobile/lib/features/coach_chat/data/feedback_providers.dart"
      via: "ref.watch(feedbackThreadProvider)"
      pattern: "feedbackThreadProvider"
    - from: "mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart"
      to: "mobile/lib/features/coach_chat/data/feedback_repository.dart"
      via: "ref.read(feedbackRepositoryProvider).submitFeedback"
      pattern: "feedbackRepositoryProvider.*submitFeedback"
    - from: "mobile/lib/features/coach_chat/presentation/coach_tab_screen.dart"
      to: "mobile/lib/features/auth/data/subscription_provider.dart"
      via: "ref.watch(isSubscribedProvider)"
      pattern: "isSubscribedProvider"
---

<objective>
Build the Coach Chat UI: premium gate screen, paywall screen, chat thread with bubbles, compose bar with photo attachment, and photo thumbnail widget.

Purpose: This is the primary FR-010 user-facing surface. Students interact with the coach through this chat screen. The premium gate enforces FR-010a privacy by ensuring only subscribed users access the feature.
Output: CoachTabScreen, CoachPaywallScreen, CoachChatScreen, ChatBubble, ComposeBar, PhotoThumbnail widgets; passing widget tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-us5-private-feedback/07-RESEARCH.md
@.planning/phases/07-us5-private-feedback/07-UI-SPEC.md
@.planning/phases/07-us5-private-feedback/07-02-SUMMARY.md

@mobile/lib/features/coach_chat/data/feedback_providers.dart
@mobile/lib/features/coach_chat/data/feedback_repository.dart
@mobile/lib/features/coach_chat/domain/feedback_message_model.dart
@mobile/lib/features/auth/data/subscription_provider.dart
@mobile/lib/shared/theme/app_theme.dart

<interfaces>
From mobile/lib/features/coach_chat/data/feedback_providers.dart:
```dart
@Riverpod(keepAlive: true)
FeedbackRepository feedbackRepository(Ref ref);
// Provider: feedbackRepositoryProvider

final feedbackThreadProvider = StreamProvider<List<LocalFeedbackThread>>((ref) { ... });
final coachRepliesProvider = StreamProvider<List<LocalFeedbackThread>>((ref) { ... });
```

From mobile/lib/features/auth/data/subscription_provider.dart:
```dart
@riverpod
Future<bool> isSubscribed(Ref ref) async { ... }
// Provider: isSubscribedProvider — returns AsyncValue<bool>
```

From mobile/lib/features/coach_chat/domain/feedback_message_model.dart:
```dart
const String kGeneralSessionId = '00000000-0000-0000-0000-000000000000';
enum FeedbackStatus { sent, pending }
```

From mobile/lib/core/database/tables/feedback_threads_table.dart (after Plan 01):
```dart
class LocalFeedbackThreads extends Table {
  TextColumn get id => text()();
  TextColumn get studentId => text()();
  TextColumn get sessionId => text()();
  TextColumn get studentMessage => text()();
  TextColumn get photoUrl => text().nullable()();
  TextColumn get coachReply => text().nullable()();
  DateTimeColumn get repliedAt => dateTime().nullable()();
  BoolColumn get notificationSent => boolean().withDefault(const Constant(false))();
  DateTimeColumn get createdAt => dateTime()();
  DateTimeColumn get updatedAt => dateTime()();
  TextColumn get status => text().withDefault(const Constant('sent'))();
  TextColumn get localPhotoPath => text().nullable()();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoachPaywallScreen + CoachTabScreen + ChatBubble + PhotoThumbnail widgets</name>
  <files>
    mobile/lib/features/coach_chat/presentation/coach_paywall_screen.dart,
    mobile/lib/features/coach_chat/presentation/coach_tab_screen.dart,
    mobile/lib/features/coach_chat/presentation/widgets/chat_bubble.dart,
    mobile/lib/features/coach_chat/presentation/widgets/photo_thumbnail.dart
  </files>
  <read_first>
    mobile/lib/features/auth/data/subscription_provider.dart,
    mobile/lib/features/auth/presentation/paywall_screen.dart,
    mobile/lib/shared/theme/app_theme.dart,
    mobile/lib/core/database/tables/feedback_threads_table.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/coach_chat/presentation/coach_paywall_screen.dart`:
   - StatelessWidget
   - Scaffold with AppBar title "Coach"
   - Center column layout with:
     - Icon: `Icons.lock_outline`, size 48, `Colors.grey[400]`
     - SizedBox(height: 16)
     - Text "Coach Chat" — `headlineMedium`, `onSurface`
     - SizedBox(height: 8)
     - Text "Available on Premium" — `bodyLarge`, `onSurfaceVariant`
     - SizedBox(height: 16)
     - Text "Get direct feedback from your coach after every session." — `bodyMedium`, `onSurfaceVariant`, textAlign center, horizontal padding 24
     - SizedBox(height: 32)
     - Padding(horizontal: 24) -> SizedBox(width: double.infinity, height: 48) -> FilledButton "Upgrade to Premium" that navigates to `/paywall` using `context.goNamed('paywall')`
   - FilledButton semanticLabel: "Upgrade to Premium subscription"
   - Lock icon wrapped in ExcludeSemantics (decorative)

2. Create `mobile/lib/features/coach_chat/presentation/coach_tab_screen.dart`:
   - ConsumerWidget
   - Watches `isSubscribedProvider`
   - Uses `.when(data: (sub) => sub ? CoachChatScreen() : CoachPaywallScreen(), loading: () => Scaffold with centered CircularProgressIndicator, error: (_, __) => CoachPaywallScreen())`

3. Create `mobile/lib/features/coach_chat/presentation/widgets/chat_bubble.dart`:
   - StatelessWidget accepting `LocalFeedbackThread thread` and `bool isCoach` and `String studentId`
   - When `isCoach` is true (thread.coachReply is not null): left-aligned, background `colorScheme.surfaceContainerHighest`, bottom-left radius 4px (rest 12px), text is `thread.coachReply!`
   - When `isCoach` is false: right-aligned, background `colorScheme.primaryContainer`, bottom-right radius 4px (rest 12px), text is `thread.studentMessage`
   - Max width: `MediaQuery.sizeOf(context).width * 0.72`
   - Padding: 12 horizontal, 8 vertical
   - Text style: `bodyLarge`
   - Above text: if `thread.photoUrl != null` or `thread.localPhotoPath != null`, show `PhotoThumbnail`
   - Below bubble: sender label + timestamp row
     - Coach: `"[Coach] ${_formatTime(thread.repliedAt!)}"` left-aligned, `bodyMedium`, `onSurfaceVariant`
     - Student: `"${_formatTime(thread.createdAt)} [You]"` right-aligned, `bodyMedium`, `onSurfaceVariant`
   - If `thread.status == 'pending'`: show `Icon(Icons.schedule, size: 12)` beside the student timestamp
   - Semantics: `Semantics(label: "[Coach/You] at [time]: [message text]")`

4. Create `mobile/lib/features/coach_chat/presentation/widgets/photo_thumbnail.dart`:
   - StatelessWidget accepting `String? photoUrl`, `String? localPhotoPath`
   - Container 160x120, borderRadius 8
   - If localPhotoPath is not null: `Image.file(File(localPhotoPath), fit: BoxFit.cover, width: 160, height: 120)`
   - If photoUrl is not null: `Image.network` with Supabase Storage signed URL (or just use the path as a placeholder for now — the actual signed URL generation can be a later concern)
   - Wrap in GestureDetector: onTap shows `showDialog` with `InteractiveViewer` containing full-size image
   - Semantics: `Semantics(label: "Attached photo, tap to view full size")`
   - While loading: show centered CircularProgressIndicator in a 160x120 container
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter analyze lib/features/coach_chat/presentation/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - coach_paywall_screen.dart contains `Icons.lock_outline`
    - coach_paywall_screen.dart contains text `'Upgrade to Premium'`
    - coach_paywall_screen.dart contains text `'Coach Chat'`
    - coach_paywall_screen.dart contains text `'Available on Premium'`
    - coach_tab_screen.dart contains `isSubscribedProvider`
    - coach_tab_screen.dart contains `CoachChatScreen` and `CoachPaywallScreen`
    - chat_bubble.dart contains `bool isCoach`
    - chat_bubble.dart contains `MediaQuery.sizeOf(context).width * 0.72`
    - chat_bubble.dart contains `Icons.schedule`
    - photo_thumbnail.dart contains `InteractiveViewer`
    - photo_thumbnail.dart contains `160` and `120` (dimensions)
    - `flutter analyze lib/features/coach_chat/presentation/` exits clean
  </acceptance_criteria>
  <done>CoachTabScreen premium gate, CoachPaywallScreen with exact UI-SPEC copy, ChatBubble with coach/student variants and pending indicator, PhotoThumbnail with tap-to-expand all compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: CoachChatScreen + ComposeBar + widget tests</name>
  <files>
    mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart,
    mobile/lib/features/coach_chat/presentation/widgets/compose_bar.dart,
    mobile/test/widget/coach_chat_screen_test.dart
  </files>
  <read_first>
    mobile/lib/features/coach_chat/data/feedback_providers.dart,
    mobile/lib/features/coach_chat/data/feedback_repository.dart,
    mobile/lib/features/coach_chat/domain/feedback_message_model.dart,
    mobile/lib/features/coach_chat/presentation/widgets/chat_bubble.dart,
    mobile/lib/core/database/tables/feedback_threads_table.dart,
    mobile/test/widget/coach_chat_screen_test.dart
  </read_first>
  <action>
1. Create `mobile/lib/features/coach_chat/presentation/widgets/compose_bar.dart`:
   - StatefulWidget
   - Takes `ValueChanged<({String message, String? photoPath})> onSend` callback
   - Layout: Row inside Container with top border (1px colorScheme.outlineVariant), background colorScheme.surface
   - Contents: Expanded TextField + camera IconButton + send FilledButton (icon only)
   - TextField: hint "Message coach...", OutlineInputBorder borderRadius 24, bodyLarge, onSurfaceVariant hint color
   - Camera button: `Icons.photo_camera_outlined`, 48x48 tap target, semanticLabel "Attach photo"
     - onPressed: use `ImagePicker().pickImage(source: ImageSource.gallery)` (returns XFile?)
     - Store selected file path in local state; show small preview chip below TextField if photo selected
   - Send button: `Icons.send`, 44x44, FilledButton.icon or IconButton with filled style
     - Enabled only when `_textController.text.isNotEmpty || _selectedPhotoPath != null`
     - Disabled state: `colorScheme.onSurface.withValues(alpha: 0.38)`
     - Enabled state: `colorScheme.primary`
     - semanticLabel: "Send message"
     - onPressed: calls `onSend((message: _textController.text, photoPath: _selectedPhotoPath))`, then clears state
   - Wrap in Padding with `MediaQuery.viewInsets.bottom` for keyboard awareness
   - Minimum height: 56px (SafeArea-aware)

2. Create `mobile/lib/features/coach_chat/presentation/coach_chat_screen.dart`:
   - ConsumerStatefulWidget
   - Scaffold with AppBar title "Coach"
   - Body: Column with Expanded ListView + ComposeBar at bottom
   - Watches `feedbackThreadProvider`:
     - `.when(data: (threads) => ..., loading: ..., error: ...)`
     - If threads is empty: show single welcome coach bubble with text "Hi! Complete a session and send me a note — I'd love to hear how it went. 🌿" (welcome message from UI-SPEC)
     - If threads is not empty: `ListView.builder` with `reverse: true` (newest at bottom), each item is ChatBubble
       - For each thread: show student bubble (isCoach: false)
       - If thread.coachReply != null: also show coach reply bubble (isCoach: true)
       - Order: by createdAt ascending (reverse ListView handles scroll position)
   - ComposeBar onSend handler:
     - Read `feedbackRepositoryProvider`
     - Check connectivity (can use a simple try/catch or check `connectivityProvider`)
     - If online and photoPath != null: call `repo.uploadPhoto(File(photoPath))` to get storage path, then `repo.submitFeedback(sessionId: kGeneralSessionId, message: msg, photoUrl: storagePath)`
     - If offline or no photo: call `repo.submitFeedback(sessionId: kGeneralSessionId, message: msg, localPhotoPath: photoPath, isOnline: false)` (offline path stores localPhotoPath)
     - Simplified approach: always set isOnline based on connectivity check; if photo and offline, store localPhotoPath
   - FcmService initialization: In initState or via a ref.listen on auth state, call `FcmService.initialize()` and `FcmService.registerToken(studentId)` once. Use a `_fcmInitialized` bool guard to prevent double-init.

3. Fill in `mobile/test/widget/coach_chat_screen_test.dart`:
   - Override `feedbackThreadProvider` with mock stream data
   - Override `feedbackRepositoryProvider` with mock FeedbackRepository
   - Override `isSubscribedProvider` with `AsyncValue.data(true)`
   - Test: renders chat bubbles when stream emits data (find ChatBubble widgets)
   - Test: pending message shows Icons.schedule
   - Test: compose bar send button disabled when empty
   - Test: compose bar send button enabled when text entered
   - Use ProviderScope.overrides pattern from Phase 6 tests
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test test/widget/coach_chat_screen_test.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - coach_chat_screen.dart contains `feedbackThreadProvider`
    - coach_chat_screen.dart contains `feedbackRepositoryProvider`
    - coach_chat_screen.dart contains `kGeneralSessionId`
    - coach_chat_screen.dart contains `ComposeBar(`
    - coach_chat_screen.dart contains `ChatBubble(`
    - compose_bar.dart contains `'Message coach'` (hint text without ellipsis or with)
    - compose_bar.dart contains `Icons.photo_camera_outlined`
    - compose_bar.dart contains `Icons.send`
    - compose_bar.dart contains `ImagePicker`
    - coach_chat_screen_test.dart exits 0 with all tests passing
  </acceptance_criteria>
  <done>CoachChatScreen renders chat thread from feedbackThreadProvider, handles empty/welcome state, ComposeBar with photo picker and send button, widget tests pass with mocked providers.</done>
</task>

</tasks>

<verification>
- `cd mobile && flutter analyze lib/features/coach_chat/presentation/` exits clean
- `cd mobile && flutter test test/widget/coach_chat_screen_test.dart` passes
- Visual: CoachTabScreen correctly routes premium vs basic users
- Copy matches UI-SPEC exactly (paywall heading, compose hint, welcome message)
</verification>

<success_criteria>
- CoachTabScreen gates premium/basic via isSubscribedProvider with loading state
- CoachPaywallScreen has exact copy from UI-SPEC (Coach Chat, Available on Premium, description, CTA)
- CoachChatScreen renders bubbles from feedbackThreadProvider with correct alignment/colors
- ChatBubble handles coach (left, surfaceContainerHighest) and student (right, primaryContainer) variants
- Pending messages show clock icon
- ComposeBar enables/disables send based on content
- Photo picker integrated with image_picker
- PhotoThumbnail shows 160x120 with tap-to-expand
- Welcome message shown for empty state
- Widget tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-us5-private-feedback/07-04-SUMMARY.md`
</output>
