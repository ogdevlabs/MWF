---
phase: 06-us4-metrics-progress
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mobile/test/unit/features/metrics/metric_delta_test.dart
  - mobile/test/unit/features/metrics/metric_repository_test.dart
  - mobile/test/unit/features/metrics/offline_metric_sync_test.dart
  - mobile/test/widget/metric_log_bottom_sheet_test.dart
  - mobile/test/widget/progress_screen_test.dart
  - mobile/test/unit/features/session/streak_test.dart
autonomous: true
requirements:
  - FR-008
  - FR-009

must_haves:
  truths:
    - "All new test files exist and compile without error"
    - "Tests initially fail (RED phase) because implementation does not yet exist"
    - "Existing 62-test suite still passes"
  artifacts:
    - path: "mobile/test/unit/features/metrics/metric_delta_test.dart"
      provides: "computeMetricDelta pure function test stubs"
    - path: "mobile/test/unit/features/metrics/metric_repository_test.dart"
      provides: "MetricRepository unit test stubs"
    - path: "mobile/test/unit/features/metrics/offline_metric_sync_test.dart"
      provides: "Offline metric log -> sync_queue -> replay integration test stubs"
    - path: "mobile/test/widget/metric_log_bottom_sheet_test.dart"
      provides: "MetricLogBottomSheet widget test stubs"
    - path: "mobile/test/widget/progress_screen_test.dart"
      provides: "ProgressScreen empty state + streak card widget test stubs"
    - path: "mobile/test/unit/features/session/streak_test.dart"
      provides: "computeLongestStreak test group appended to existing file"
  key_links:
    - from: "metric_repository_test.dart"
      to: "MetricRepository (Plan 02)"
      via: "import of metric_repository.dart"
      pattern: "import.*metric_repository"
    - from: "streak_test.dart"
      to: "streak_calculator.dart"
      via: "import of computeLongestStreak"
      pattern: "computeLongestStreak"
---

<objective>
Create all Wave 0 test stubs for Phase 6. These files define the expected behavior contracts that Plans 02-04 will implement against. Tests will initially fail (import errors or assertion failures) until implementation lands.

Purpose: Nyquist compliance — every implementation task has a pre-existing test file to run against. Defines the exact function signatures, class APIs, and widget structure before any production code is written.

Output: 6 test files (5 new + 1 extended) that compile once their corresponding production imports exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-us4-metrics-progress/06-CONTEXT.md
@.planning/phases/06-us4-metrics-progress/06-RESEARCH.md
@.planning/phases/06-us4-metrics-progress/06-VALIDATION.md

@mobile/lib/core/database/daos/metric_logs_dao.dart
@mobile/lib/core/database/tables/metric_logs_table.dart
@mobile/lib/features/session/data/streak_calculator.dart
@mobile/test/unit/features/session/streak_test.dart
@mobile/test/unit/features/session/offline_sync_integration_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs (metric_delta, metric_repository, offline_sync, streak longest)</name>
  <files>
    mobile/test/unit/features/metrics/metric_delta_test.dart
    mobile/test/unit/features/metrics/metric_repository_test.dart
    mobile/test/unit/features/metrics/offline_metric_sync_test.dart
    mobile/test/unit/features/session/streak_test.dart
  </files>
  <read_first>
    mobile/test/unit/features/session/streak_test.dart
    mobile/test/unit/features/session/offline_sync_integration_test.dart
    mobile/lib/core/database/daos/metric_logs_dao.dart
    mobile/lib/core/database/tables/metric_logs_table.dart
    mobile/lib/features/session/data/streak_calculator.dart
    mobile/lib/core/sync/sync_queue.dart
  </read_first>
  <action>
1. Create directory: `mobile/test/unit/features/metrics/`

2. Create `metric_delta_test.dart`:
   - Import `package:flutter_test/flutter_test.dart`
   - Import `package:mwf_mobile/features/metrics/domain/metric_delta.dart` (will not exist yet)
   - group('computeMetricDelta'):
     - test: 'returns null for empty list' -> expect(computeMetricDelta([]), isNull)
     - test: 'returns null for single log' -> expect(computeMetricDelta([singleLog]), isNull)
     - test: 'returns negative delta for weight decrease' -> two logs: first value=72.0, last value=69.7, expect(computeMetricDelta(logs), closeTo(-2.3, 0.01))
     - test: 'returns positive delta for flexibility increase' -> first=15.0, last=22.0, expect result closeTo(7.0, 0.01)
     - test: 'returns 0.0 for no change' -> first=70.0, last=70.0, expect 0.0
   - Use a local helper class `_FakeMetricLog` with fields: `double value, DateTime loggedAt` that matches the interface `computeMetricDelta` expects (a list of objects with `.value` and `.loggedAt`). The actual implementation will take `List<LocalMetricLog>` but for now use a lightweight stub. OR import the Drift-generated `LocalMetricLog` class (which is available since the table already exists).
   - Since `LocalMetricLog` IS already generated by Drift (the table exists in app_database), import:
     `import 'package:drift/drift.dart' hide isNull, isNotNull;`
     `import 'package:mwf_mobile/core/database/app_database.dart';`
   - Create test `LocalMetricLog` instances using the Drift data class constructor:
     ```dart
     LocalMetricLog(
       id: 'log-1', studentId: 's1', metricType: 'weight',
       metricSubtype: null, value: 72.0, unit: 'kg',
       loggedAt: DateTime(2026, 5, 1), createdAt: DateTime(2026, 5, 1),
     )
     ```

3. Create `metric_repository_test.dart`:
   - Import `package:drift/drift.dart` hide isNull, isNotNull
   - Import `package:drift/native.dart`
   - Import `package:flutter_test/flutter_test.dart`
   - Import `package:mocktail/mocktail.dart`
   - Import `package:mwf_mobile/core/database/app_database.dart`
   - Import `package:mwf_mobile/core/sync/sync_queue.dart`
   - Import `package:mwf_mobile/features/metrics/data/metric_repository.dart` (will not exist yet)
   - class MockSyncQueue extends Mock implements SyncQueue {}
   - setUp: db = AppDatabase(NativeDatabase.memory()), mockSyncQueue = MockSyncQueue()
   - tearDown: await db.close()
   - registerFallbackValue in setUpAll for Map<String, dynamic> type
   - group('MetricRepository.logMetric'):
     - test: 'inserts into local_metric_logs via Drift' -> call repo.logMetric(metricType: 'weight', value: 75.0, unit: 'kg', loggedAt: DateTime(2026,5,28)), then db.metricLogsDao.getMetricsByStudent('student-1') returns list with length 1
     - test: 'enqueues to sync_queue with target table metric_logs' -> verify(() => mockSyncQueue.enqueue(operation: 'insert', targetTable: 'metric_logs', payload: any(named: 'payload'))).called(1)
     - test: 'payload logged_at is YYYY-MM-DD date string not full ISO-8601' -> use captureAny on the payload named param, extract 'logged_at' value, expect it matches RegExp(r'^\d{4}-\d{2}-\d{2}$')
   - Wire: MetricRepository(db: db, syncQueue: mockSyncQueue, studentId: 'student-1')
   - Mock setup: when(() => mockSyncQueue.enqueue(operation: any(named: 'operation'), targetTable: any(named: 'targetTable'), payload: any(named: 'payload'))).thenAnswer((_) async {})

4. Create `offline_metric_sync_test.dart`:
   - Mirror the pattern from `offline_sync_integration_test.dart` exactly
   - Import drift/dart hide isNull, isNotNull; drift/native; flutter_test; mocktail; postgrest; supabase_flutter
   - Import app_database, sync_queue, metric_repository
   - Declare MockSupabaseClient, _FakeQueryBuilder (copy pattern from offline_sync_integration_test.dart), _CompletedFilterBuilder
   - group('Offline metric log -> sync -> replay'):
     - test: 'logMetric inserts locally and enqueues, processQueue replays to Supabase'
       1. Create AppDatabase(NativeDatabase.memory())
       2. Create MockSupabaseClient + _FakeQueryBuilder for 'metric_logs'
       3. Create real SyncQueue(db: db, supabase: mockSupabase)
       4. Create MetricRepository(db: db, syncQueue: syncQueue, studentId: 'student-1')
       5. Call repo.logMetric(metricType: 'weight', value: 75.0, unit: 'kg', loggedAt: DateTime(2026,5,28))
       6. Assert: db.metricLogsDao.getMetricsByStudent('student-1') has length 1
       7. Assert: db.syncQueueDao.getPendingItems() has length 1
       8. Wire: when(() => mockSupabase.from('metric_logs')).thenReturn(fakeQueryBuilder)
       9. Call syncQueue.processQueue()
       10. Assert: db.syncQueueDao.getPendingItems() is empty
       11. Assert: fakeQueryBuilder.upsertedPayloads.first['metric_type'] == 'weight'
       12. Assert: fakeQueryBuilder.upsertedPayloads.first['logged_at'] == '2026-05-28'

5. Append to existing `streak_test.dart`:
   - Add import for computeLongestStreak (same file `streak_calculator.dart`)
   - Add new group AFTER the existing `computeCurrentStreak` group:
   - group('computeLongestStreak'):
     - test: 'returns 0 for empty list' -> expect(computeLongestStreak([]), 0)
     - test: 'returns 1 for single date' -> expect(computeLongestStreak([DateTime(2026,5,28)]), 1)
     - test: 'returns 2 for two consecutive days' -> [DateTime(2026,5,27), DateTime(2026,5,28)] -> 2
     - test: 'returns longest run when gap in middle' -> [May 1,2,3, May 10,11,12,13,14] -> longest is 5
     - test: 'handles duplicates on same day' -> [May 1 08:00, May 1 20:00, May 2, May 3] -> 3
     - test: 'unsorted input still finds longest' -> pass dates out of order, same expectations

CRITICAL: The Supabase table name is `metric_logs` (confirmed in migration file `001_initial_schema.sql` line 98). The CONTEXT.md D-16 references `body_metrics` which is INCORRECT. Use `metric_logs` as the targetTable in all sync operations.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test test/unit/features/session/streak_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: mobile/test/unit/features/metrics/metric_delta_test.dart
    - File exists: mobile/test/unit/features/metrics/metric_repository_test.dart
    - File exists: mobile/test/unit/features/metrics/offline_metric_sync_test.dart
    - streak_test.dart contains string "computeLongestStreak"
    - streak_test.dart existing tests still pass (group computeCurrentStreak unmodified)
    - metric_repository_test.dart contains string "targetTable: 'metric_logs'"
    - offline_metric_sync_test.dart contains string "'logged_at': '2026-05-28'"
  </acceptance_criteria>
  <done>
    - 4 test files created in test/unit/features/metrics/ and test/unit/features/session/
    - streak_test.dart extended with computeLongestStreak group (existing tests still green)
    - All new metric test files reference correct table name 'metric_logs' (not 'body_metrics')
    - Payload assertions verify YYYY-MM-DD date format
  </done>
</task>

<task type="auto">
  <name>Task 2: Create widget test stubs (MetricLogBottomSheet, ProgressScreen)</name>
  <files>
    mobile/test/widget/metric_log_bottom_sheet_test.dart
    mobile/test/widget/progress_screen_test.dart
  </files>
  <read_first>
    mobile/test/widget/session_player_screen_test.dart
    mobile/test/widget/session_list_tile_download_test.dart
    mobile/lib/core/database/app_database.dart
  </read_first>
  <action>
1. Create `mobile/test/widget/metric_log_bottom_sheet_test.dart`:
   - Import flutter/material, flutter_riverpod, flutter_test
   - Import the future widget: `package:mwf_mobile/features/metrics/presentation/metric_log_bottom_sheet.dart`
   - group('MetricLogBottomSheet'):
     - test: 'renders metric type selector' -> pump MetricLogBottomSheet inside MaterialApp + ProviderScope + Scaffold, find.byType(DropdownButton) or find.text('Weight') — expect findsOneWidget
     - test: 'renders value text field with numeric keyboard hint' -> find.byType(TextField) findsAtLeastNWidgets(1)
     - test: 'renders date field defaulting to today' -> find.text containing today's formatted date
     - test: 'renders Log another button' -> find.text('Log another') findsOneWidget
     - test: 'dismiss does not crash' -> tester.tapAt(Offset(0, 0)) or Navigator.pop, pumpAndSettle, no errors
   - Wrap the widget in: MaterialApp(home: Scaffold(body: Builder(builder: (ctx) { showModalBottomSheet(context: ctx, isScrollControlled: true, builder: (_) => const MetricLogBottomSheet()); return const SizedBox(); })))
   - Alternative simpler approach: Just pump MetricLogBottomSheet directly in a Scaffold since it's the sheet content widget.
   - Override any required providers with mock/fake values.

2. Create `mobile/test/widget/progress_screen_test.dart`:
   - Import flutter/material, flutter_riverpod, flutter_test
   - Import `package:mwf_mobile/features/metrics/presentation/progress_screen.dart`
   - group('ProgressScreen'):
     - test: 'shows empty state message when no metric logs exist' -> override metric stream providers to return empty list, override streak provider to return (current: 0, longest: 0), pump ProgressScreen, expect find.text('No data yet') or similar empty state text findsOneWidget
     - test: 'shows streak card with current and longest streak values' -> override streak providers to return current=5, longest=12, pump ProgressScreen, expect find.text('5') and find.text('12') both findsOneWidget
     - test: 'shows tab bar with Weight, Measurements, Flexibility tabs' -> pump ProgressScreen, expect find.text('Weight'), find.text('Measurements'), find.text('Flexibility') all findsOneWidget
     - test: 'shows Log Metrics button' -> find.text('Log Metrics') findsOneWidget
   - Wrap widget: MaterialApp(home: ProviderScope(overrides: [...], child: ProgressScreen()))
   - Do NOT test fl_chart LineChart rendering — only test widget tree structure
   - For provider overrides, create simple StreamProvider overrides that emit empty lists

NOTE: These tests will NOT compile until Plan 03 creates the ProgressScreen and MetricLogBottomSheet widgets. This is expected — Wave 0 defines contracts.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && test -f test/widget/metric_log_bottom_sheet_test.dart && test -f test/widget/progress_screen_test.dart && echo "PASS: widget test files exist"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: mobile/test/widget/metric_log_bottom_sheet_test.dart
    - File exists: mobile/test/widget/progress_screen_test.dart
    - metric_log_bottom_sheet_test.dart contains string "MetricLogBottomSheet"
    - progress_screen_test.dart contains string "ProgressScreen"
    - progress_screen_test.dart contains string "No data yet"
    - progress_screen_test.dart contains string "Log Metrics"
    - progress_screen_test.dart does NOT contain "LineChart" rendering assertions (only find.byType at most)
    - Neither file imports fl_chart directly
  </acceptance_criteria>
  <done>
    - 2 widget test files created defining the UI contracts
    - ProgressScreen tests verify: empty state message, streak card values, tab bar labels, Log Metrics button
    - MetricLogBottomSheet tests verify: form fields present, dismiss behavior
    - No fl_chart rendering tested (per research: test data layer only)
  </done>
</task>

</tasks>

<verification>
- `flutter test test/unit/features/session/streak_test.dart --no-pub` passes (existing tests + new computeLongestStreak group that will fail until Plan 02 adds the function)
- All 6 test files exist on disk
- Existing full suite (`flutter test --no-pub`) still passes (62 tests) — new files that import non-existent code are expected to have compile errors but won't be picked up unless explicitly targeted
</verification>

<success_criteria>
- 6 test files exist (5 new + 1 extended)
- streak_test.dart existing 8 tests still pass
- New test files define exact function signatures: computeMetricDelta(List<LocalMetricLog>), computeLongestStreak(List<DateTime>), MetricRepository.logMetric()
- All sync-related tests use table name 'metric_logs' (not 'body_metrics')
- All logged_at assertions verify YYYY-MM-DD format
</success_criteria>

<output>
After completion, create `.planning/phases/06-us4-metrics-progress/06-01-SUMMARY.md`
</output>
