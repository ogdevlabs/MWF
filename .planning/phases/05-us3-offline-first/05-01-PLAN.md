---
phase: 05-us3-offline-first
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mobile/test/unit/core/downloads/storage_guard_test.dart
  - mobile/test/unit/core/sync/sync_service_stale_video_test.dart
  - mobile/test/unit/features/session/session_download_state_test.dart
  - mobile/test/unit/features/session/offline_sync_integration_test.dart
  - mobile/test/widget/session_list_tile_download_test.dart
autonomous: true
requirements: [FR-006, FR-007]

must_haves:
  truths:
    - "All 5 Wave 0 test files exist and compile (red state — tests fail but code compiles)"
    - "flutter test runs without analysis errors on new test files"
  artifacts:
    - path: "mobile/test/unit/core/downloads/storage_guard_test.dart"
      provides: "StorageGuard threshold tests with injectable freeSpaceProvider"
      contains: "StorageGuard.hasEnoughSpace"
    - path: "mobile/test/unit/core/sync/sync_service_stale_video_test.dart"
      provides: "Stale version detection + videoLocalPath clearing test"
      contains: "downloadStatus.*pending"
    - path: "mobile/test/unit/features/session/session_download_state_test.dart"
      provides: "Per-session download state derivation (all complete, in_progress, none)"
      contains: "SessionDownloadState"
    - path: "mobile/test/unit/features/session/offline_sync_integration_test.dart"
      provides: "Offline completion -> sync_queue -> replay on reconnect"
      contains: "processQueue"
    - path: "mobile/test/widget/session_list_tile_download_test.dart"
      provides: "Badge rendering + offline-unavailable state"
      contains: "Not available offline"
  key_links:
    - from: "test stubs"
      to: "feature code in Plans 02-04"
      via: "import paths referencing not-yet-created production code"
      pattern: "import.*storage_guard|import.*download_state_provider|import.*session_download_state"
---

<objective>
Create all Wave 0 test stub files required by the Nyquist Validation Strategy.

Purpose: These test files must exist before feature code so the TDD-adjacent red-green cycle
works — feature implementation in Plans 02-04 will turn these tests green.

Output: 5 new test files that import the production code paths (which don't yet exist),
define test groups with `skip: true` annotations so `flutter test` doesn't hard-fail,
and document the exact behaviors to verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-us3-offline-first/05-CONTEXT.md
@.planning/phases/05-us3-offline-first/05-RESEARCH.md
@.planning/phases/05-us3-offline-first/05-VALIDATION.md

@mobile/test/unit/core/downloads/download_service_test.dart
@mobile/test/unit/core/sync/sync_queue_test.dart
@mobile/test/unit/features/session/session_completion_test.dart

<interfaces>
<!-- Existing test patterns for reference -->

From mobile/test/unit/core/sync/sync_queue_test.dart:
```dart
import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:mwf_mobile/core/database/app_database.dart';
import 'package:mwf_mobile/core/sync/sync_queue.dart';

class MockSupabaseClient extends Mock implements SupabaseClient {}

// Pattern: in-memory Drift + MockSupabaseClient
```

From mobile/test/unit/core/downloads/download_service_test.dart:
```dart
import 'package:drift/drift.dart' hide isNull, isNotNull;
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mwf_mobile/core/database/app_database.dart';
import 'package:mwf_mobile/core/downloads/download_service.dart';

// Pattern: hide isNull/isNotNull from drift to avoid matcher conflict
// Do NOT call service.initialize() — FileDownloader needs platform channels
```

From mobile/lib/core/database/daos/download_manifest_dao.dart:
```dart
Future<void> upsertEntry(DownloadManifestCompanion entry) =>
    into(downloadManifest).insertOnConflictUpdate(entry);
Future<DownloadManifestData?> getByExerciseId(String exerciseId) => ...;
Future<List<DownloadManifestData>> getPendingDownloads() => ...;
Stream<List<DownloadManifestData>> watchAllEntries() => ...;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs (StorageGuard, stale video, download state, offline sync)</name>
  <files>
    mobile/test/unit/core/downloads/storage_guard_test.dart
    mobile/test/unit/core/sync/sync_service_stale_video_test.dart
    mobile/test/unit/features/session/session_download_state_test.dart
    mobile/test/unit/features/session/offline_sync_integration_test.dart
  </files>
  <read_first>
    mobile/test/unit/core/sync/sync_queue_test.dart
    mobile/test/unit/core/downloads/download_service_test.dart
    mobile/test/unit/features/session/session_completion_test.dart
    mobile/lib/core/database/app_database.dart
    mobile/lib/core/database/daos/download_manifest_dao.dart
    mobile/lib/core/sync/sync_service.dart
  </read_first>
  <action>
Create 4 test files, all using `skip: 'Wave 0 stub — production code not yet created'` on the group so `flutter test` passes without errors:

**1. `mobile/test/unit/core/downloads/storage_guard_test.dart`**
- Import `package:flutter_test/flutter_test.dart`
- Group: `'StorageGuard'` with skip annotation
- Test: `'returns false when freeSpace < 500 MB threshold'` — will call `StorageGuard.hasEnoughSpace(thresholdBytes: 500 * 1024 * 1024, freeSpaceProvider: () async => 100 * 1024 * 1024)` and expect `isFalse`
- Test: `'returns true when freeSpace >= 500 MB threshold'` — freeSpaceProvider returns `1024 * 1024 * 1024` (1 GB), expect `isTrue`
- Test: `'returns true (fail-open) when freeSpaceProvider throws'` — provider throws Exception, expect `isTrue`

**2. `mobile/test/unit/core/sync/sync_service_stale_video_test.dart`**
- Import drift (hide isNull, isNotNull), drift/native, flutter_test, mocktail, app_database, sync_service, supabase_flutter
- Create `MockSupabaseClient extends Mock implements SupabaseClient {}`
- setUp: in-memory `AppDatabase`, `MockSupabaseClient`, `SyncQueue`, `SyncService`
- Group: `'Stale video detection'` with skip annotation
- Test: `'resets manifest to pending when remote video_version > local'` — seed manifest with exerciseId='ex-1', videoVersion=1, downloadStatus='complete', videoLocalPath='exercises/ex-1/ex-1_video.mp4'. Mock Supabase to return exercise row with video_version=2. Call `syncService.sync()`. Assert manifest downloadStatus == 'pending'.
- Test: `'clears videoLocalPath with Value(null) not Value.absent()'` — same setup, after sync assert `manifest.videoLocalPath == null`
- Test: `'updates videoVersion to remote version'` — assert manifest.videoVersion == 2

**3. `mobile/test/unit/features/session/session_download_state_test.dart`**
- Import flutter_test, drift, drift/native, app_database
- setUp: in-memory AppDatabase
- Group: `'SessionDownloadState derivation'` with skip annotation
- Test: `'returns downloaded when all exercises have complete manifest'`
- Test: `'returns inProgress when any exercise has in_progress manifest'`
- Test: `'returns notDownloaded when no manifest entries exist for session exercises'`
- Test: `'returns notDownloaded when relevant entries list is empty (vacuous every guard)'`

**4. `mobile/test/unit/features/session/offline_sync_integration_test.dart`**
- Import drift, drift/native, flutter_test, mocktail, supabase_flutter, app_database, sync_queue
- setUp: in-memory AppDatabase, MockSupabaseClient, SyncQueue
- Group: `'Offline sync integration'` with skip annotation
- Test: `'offline completion enqueues to sync_queue and replays on reconnect'` — enqueue a progress_record, mock supabase.from('progress_records').upsert() success, call processQueue(), assert queue empty after replay
- Test: `'sync queue processes items in FIFO order'` — enqueue 2 items with different createdAt, verify first processed before second

All tests use `group('...', skip: 'Wave 0 stub — production code not yet created', () {...})` pattern so `flutter test` passes.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test test/unit/core/downloads/storage_guard_test.dart test/unit/core/sync/sync_service_stale_video_test.dart test/unit/features/session/session_download_state_test.dart test/unit/features/session/offline_sync_integration_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `mobile/test/unit/core/downloads/storage_guard_test.dart` exists and contains `StorageGuard.hasEnoughSpace`
    - File `mobile/test/unit/core/sync/sync_service_stale_video_test.dart` exists and contains `downloadStatus` and `pending`
    - File `mobile/test/unit/features/session/session_download_state_test.dart` exists and contains `SessionDownloadState`
    - File `mobile/test/unit/features/session/offline_sync_integration_test.dart` exists and contains `processQueue`
    - `flutter test` on all 4 files exits 0 (tests skipped, not failed)
    - Each file contains `skip:` annotation on the group
  </acceptance_criteria>
  <done>4 unit test stubs exist, compile, and pass (skipped) under `flutter test`</done>
</task>

<task type="auto">
  <name>Task 2: Create widget test stub for SessionListTile download badge</name>
  <files>
    mobile/test/widget/session_list_tile_download_test.dart
  </files>
  <read_first>
    mobile/lib/features/session/presentation/session_list_tile.dart
    mobile/lib/features/session/domain/session_model.dart
    mobile/test/unit/features/session/session_completion_test.dart
  </read_first>
  <action>
Create `mobile/test/widget/session_list_tile_download_test.dart`:
- Create `test/widget/` directory if it doesn't exist
- Import flutter/material.dart, flutter_test, session_list_tile, session_model
- Group: `'SessionListTile download state'` with `skip: 'Wave 0 stub — SessionListTile download params not yet added'`
- Test: `'shows download_outlined icon when downloadState is notDownloaded'` — pump `SessionListTile` with `downloadState: SessionDownloadState.notDownloaded`, `isOnline: true`, expect `find.byIcon(Icons.download_outlined)` findsOneWidget
- Test: `'shows circular progress when downloadState is inProgress'` — pump with `downloadState: SessionDownloadState.inProgress`, expect `find.byType(CircularProgressIndicator)` findsOneWidget
- Test: `'shows check icon when downloadState is downloaded'` — pump with `downloadState: SessionDownloadState.downloaded`, expect `find.byIcon(Icons.download_done)` findsOneWidget
- Test: `'shows Not available offline text when offline and not downloaded'` — pump with `isOnline: false`, `downloadState: SessionDownloadState.notDownloaded`, expect `find.text('Not available offline')` findsOneWidget
- Test: `'tile onTap is null when offline and not downloaded'` — same state, find ListTile, expect `tile.onTap` isNull
- Test: `'tile onTap is active when online and not downloaded'` — isOnline: true, expect onTap is not null

All wrapped in skip group so flutter test passes.
  </action>
  <verify>
    <automated>cd /Users/oglabs/source/fererelabs/mwf/mobile && flutter test test/widget/session_list_tile_download_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `mobile/test/widget/session_list_tile_download_test.dart` exists
    - Contains `'Not available offline'` string
    - Contains `SessionDownloadState` reference
    - Contains `skip:` annotation
    - `flutter test test/widget/session_list_tile_download_test.dart` exits 0
  </acceptance_criteria>
  <done>Widget test stub exists, compiles, and passes (skipped) under `flutter test`</done>
</task>

</tasks>

<verification>
All 5 Wave 0 test files exist and `flutter test` on the full suite passes without new failures:
```
cd mobile && flutter test test/unit/ test/widget/
```
Expected: all existing tests still pass; new tests show as skipped.
</verification>

<success_criteria>
- 5 new test files created in correct directories
- All contain skip annotations so they don't red-fail the suite
- Each test file documents the exact behaviors to be verified by Plans 02-04
- `flutter test` exits 0 for the entire suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-us3-offline-first/05-01-SUMMARY.md`
</output>
